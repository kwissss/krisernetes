---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: deduparr
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: deduparr-secret
    template:
      data:
        DEDUPARR_RADARR_HD_API_KEY: "{{ .RADARR_HD_API_KEY }}"
        DEDUPARR_RADARR_UHD_API_KEY: "{{ .RADARR_UHD_API_KEY }}"
        DEDUPARR_SONARR_HD_API_KEY: "{{ .SONARR_HD_API_KEY }}"
        DEDUPARR_SONARR_UHD_API_KEY: "{{ .SONARR_UHD_API_KEY }}"
  dataFrom:
    - extract:
        key: radarr
    - extract:
        key: sonarr